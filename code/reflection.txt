In this lab I learned how bill.py connects to the different interfaces like console.py and dashboard.py, and how each one depends on the same functions.
 Using the unit tests helped me check each function on its own instead of running everything, which made things easier.\
 The debugger was the biggest improvement for me — setting a breakpoint in grand_total showed exactly why the assertion failed, instead of me guessing.
  I also got more comfortable committing and pushing with Git since I hadn’t really used version control much before. My next steps are to get quicker at reading assertion messages in both unit and integration tests and to use debugging as my first approach when something breaks.